<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Quyền RIÊNG, chỉ cần khai báo/xin khi app THẬT SỰ cần vị trí lúc KHÔNG
         mở (chạy nền) — ví dụ app theo dõi hành trình chạy bộ tiếp tục ghi nhận
         khi màn hình tắt. Phần lớn app (bản đồ, tìm địa điểm gần đây khi đang
         mở) KHÔNG cần quyền này — xem mục 37.3 để tự đánh giá có thật sự cần. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.HelloAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
