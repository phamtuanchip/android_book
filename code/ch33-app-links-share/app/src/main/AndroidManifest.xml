<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.HelloAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Khai báo NÀY khiến app xuất hiện trong "Share sheet" (menu Chia sẻ)
                 của các app khác khi người dùng chia sẻ văn bản — xem mục 33.2. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NoteDetailActivity"
            android:exported="true">

            <!-- Custom scheme deep link — LUÔN hoạt động, không cần xác minh domain,
                 test được ngay bằng adb (xem mục 33.3). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ch33share" android:host="note" />
            </intent-filter>

            <!-- App Link thật (http/https) — MINH HOẠ CÚ PHÁP, không hoạt động thật
                 trên máy bạn vì "example.com" không phải domain bạn sở hữu và chưa
                 host file .well-known/assetlinks.json cần thiết — xem mục 33.4. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="example.com"
                    android:pathPrefix="/note" />
            </intent-filter>
        </activity>
    </application>

</manifest>
