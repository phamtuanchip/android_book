<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.HelloAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- exported="true" + intent-filter với action tuỳ chỉnh: vì :client là
             MỘT APP KHÁC (không chung package), nó không thể bind bằng cách chỉ
             định class (new Intent(context, CalculatorService.class)) như Chương
             16 — phải bind bằng action + setPackage() tường minh, xem mục 34.3. -->
        <service
            android:name=".CalculatorService"
            android:exported="true">
            <intent-filter>
                <action android:name="vn.example.ch34aidlserver.action.BIND_CALCULATOR" />
            </intent-filter>
        </service>
    </application>

</manifest>
