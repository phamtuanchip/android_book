<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Từ Android 11 (API 30), app KHÔNG TỰ THẤY được app khác đã cài trên máy
         (chống app do thám danh sách cài đặt) — muốn bindService() sang một
         package cụ thể, phải khai báo rõ ràng package đó ở đây, nếu không
         bindService() âm thầm không bao giờ gọi được onServiceConnected(). -->
    <queries>
        <package android:name="vn.example.ch34aidlserver" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.HelloAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
